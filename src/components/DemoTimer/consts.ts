import type { DemoTimerLabels } from "./types";

export const TICK_MS = 1000;

export const LABELS: DemoTimerLabels = {
  title: "Demo timer",
  start: "Start",
  pause: "Pause",
  reset: "Reset",
};
